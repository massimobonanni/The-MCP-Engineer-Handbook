[project]
name = "echo-http-server"
version = "0.1.0"
description = "Chapter 1 Streamable HTTP echo server"
requires-python = ">=3.12"
dependencies = [
    "mcp==2.0.0b1",
]

[tool.uv]
prerelease = "allow"
