[project]
name = "long-running-operation"
version = "0.1.0"
description = "Long-running operations split across multiple tools (Chapter 2, §6.4)"
requires-python = ">=3.12"
dependencies = [
    "mcp==2.0.0b1",
]

[tool.uv]
prerelease = "allow"
