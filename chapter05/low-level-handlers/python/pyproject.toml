[project]
name = "low-level-handlers"
version = "1.0.0"
description = "Low-level Server with explicit tools/list and tools/call handlers"
requires-python = ">=3.12"
dependencies = [
    "mcp==2.0.0b1",
]

[tool.uv]
prerelease = "allow"
