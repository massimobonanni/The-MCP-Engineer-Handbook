[project]
name = "schema-mapping"
version = "1.0.0"
description = "Multi-provider schema mapping demo: MCP tool definitions through five model-API adapters"
requires-python = ">=3.12"
dependencies = [
    "mcp==2.0.0b1",
]

[tool.uv]
prerelease = "allow"
